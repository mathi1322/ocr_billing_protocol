# Local infra. DB / auth / storage come from Supabase (cloud or `supabase start`).
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
